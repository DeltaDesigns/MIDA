<UserControl
    x:Class="MIDA.DialogueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MIDA"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="1000"
    d:DesignWidth="900"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ControlTemplate x:Key="DialogueTemplate">
            <Button
                Height="90"
                Margin="5"
                Click="PlayWem_OnClick"
                Style="{StaticResource MarathonStyleButton}">
                <Grid Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="80*" />
                        <RowDefinition Height="30*" />
                    </Grid.RowDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Margin="5,5,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        FontFamily="{DynamicResource PP Fraktion Mono Semibold Keys}"
                        FontSize="16"
                        Foreground="{StaticResource BackgroundColour14}"
                        Text="{Binding Voiceline}"
                        TextWrapping="WrapWithOverflow" />
                    <TextBlock
                        Grid.Row="1"
                        Margin="5,0,0,5"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        FontFamily="{DynamicResource PP Fraktion Mono Medium Keys}"
                        FontSize="14"
                        Foreground="{StaticResource BackgroundColour30}"
                        Text="{Binding Narrator}" />
                    <TextBlock
                        Grid.Row="0"
                        Margin="0,0,5,0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        FontFamily="{DynamicResource PP Fraktion Mono Medium Keys}"
                        FontSize="12"
                        Foreground="{StaticResource BackgroundColour30}"
                        Text="{Binding RecursionDepth}" />
                    <TextBlock
                        Grid.Row="1"
                        Margin="0,0,5,5"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        FontFamily="{DynamicResource PP Fraktion Mono Medium Keys}"
                        FontSize="14"
                        Foreground="{StaticResource BackgroundColour30}"
                        Text="{Binding Duration}" />
                </Grid>
            </Button>
        </ControlTemplate>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="90*" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>

        <ListView
            x:Name="ListView"
            Grid.Row="0"
            Background="Transparent"
            BorderThickness="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="OverridesDefaultStyle" Value="True" />
                    <Setter Property="Template" Value="{StaticResource DialogueTemplate}" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        <local:MusicPlayerControl x:Name="MusicPlayer" Grid.Row="1" />
    </Grid>
</UserControl>
