<UserControl
    x:Class="MIDA.ConfigView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MIDA"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    Loaded="UserControl_Loaded"
    mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="SettingsButtonTemplate">
            <RadioButton
                Margin="0"
                Checked="SettingsCategory_OnClick"
                Content="{Binding Text}"
                FontSize="30"
                GroupName="SettingsCategory"
                Style="{DynamicResource MarathonStyleTabButton}" />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.Triggers>
            <EventTrigger RoutedEvent="Grid.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation
                            Storyboard.TargetProperty="Opacity"
                            From="0"
                            To="1"
                            Duration="0:0:0.25" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15*" />
            <ColumnDefinition Width="50*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="Transparent">
            <StackPanel
                Margin="50,100,0,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top">
                <ItemsControl x:Name="Settings" ItemTemplate="{StaticResource SettingsButtonTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel
                                Margin="0,0,0,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </StackPanel>
        </Grid>

        <Grid
            x:Name="SettingsPage"
            Grid.Column="1"
            Background="Transparent">
            <Grid.RenderTransform>
                <TranslateTransform />
            </Grid.RenderTransform>

            <local:GeneralConfigView x:Name="ConfigControl" Visibility="Collapsed" />
            <local:PortingConfigView x:Name="PortingControl" Visibility="Collapsed" />

        </Grid>

    </Grid>
</UserControl>
